A windows forms app that allows the user to create different surveys questions and edit or remove them.

to generate the database and its tables run the script in the 'database generation script' file.